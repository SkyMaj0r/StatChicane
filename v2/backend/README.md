# StatChicane v2 — Backend
FastAPI backend. Houses the /api/ask (NLP pipeline), /api/racelab (FastF1 telemetry), and /api/auth (JWT auth) routers.

LLM: Google Gemini API
Database: MySQL (F1DB)
Cache: Redis
Telemetry: FastF1
